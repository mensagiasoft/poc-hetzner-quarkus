version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: src/main/docker/Dockerfile.native
    container_name: quarkus-app
    restart: always
    ports:
      - "8082:8082"
    networks:
      - app-network
networks:
  app-network:
    external: true